<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/skeleton/2.0.4/skeleton.css" />
    <title>Sign Up Page</title>
</head>

<body>
    <h1>Sign Up</h1>
    <ul style="list-style-type:none">
        <% if(typeof errors !='undefined' ) { %>
            <%errors.forEach(error=>{ %>
                <li>
                    <%= error.message %>
                </li>
                <% }) %>
                    <% } %>
    </ul>
    <a href="/">Home</a>
    <form action="/signup" method="POST">
        <div class="form-userinfo">
            <input type="text" id="firstname" name=firstName autofocus placeholder="First Name" required>
        </div>
        <div class="form-userinfo">
            <input type="text" id="lastname" name=lastName autofocus placeholder="Last Name" required>
        </div>
        <div class="form-userinfo">
            <input type="text" id="emailid" name=emailId autofocus placeholder="Email"
                pattern="[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,}$" title="Must be a valid email id" required>
        </div>
        <div class="form-userinfo">
            <input type="password" id="password" name=password autofocus placeholder="Password"
                pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}"
                title="Must contain at least one  number and one uppercase and lowercase letter, and at least 8 or more characters"
                required>
        </div>
        <div class="form-userinfo">
            <input type="password" id="confirmpassword" name=confirmPassword autofocus placeholder="Confirm Password"
                title="Must match the password" required>
        </div>
        <div>
            <button type=submit id="signIn"> Sign Up</button>
        </div>
        <a href="/signin">Already have Account? Sign In here</a>
    </form>
</body>

</html>